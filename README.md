# HTTP server

A simple HTTP server built from scratch in C++17 using Linux epoll for non-blocking I/O. This is a learning project to understand how network programming, event-driven architecture, and HTTP protocol parsing work under the hood. Not intended for production use.